export interface AttemptQuestion{ id:string;version:number;sectionId:string;sortOrder:number;points:number;type:string;text:string;imageAssetId:string;imageAlt:string;optionsEmbeddedInImage:boolean;videoUrl:string;difficulty:string;options:Array<{index:number;text:string;assetId:string}>}
export interface SavedAnswer{questionId:string;selectedOptionIndex:number|null;textAnswer:string;timeSpentSeconds:number;markedForReview:boolean;lastSavedAt:string}
export interface Attempt{id:string;assessmentId:string;assessmentVersion:number;attemptNumber:number;status:'in_progress'|'submitted'|'expired'|'abandoned';title:string;startedAt:string;expiresAt:string|null;submittedAt:string|null;showProgressBar:boolean;requireAnswerBeforeNext:boolean;allowQuestionReview:boolean;randomizeOptions:boolean;questions:AttemptQuestion[];answers:SavedAnswer[]}
export interface Result{attemptId:string;score:number;totalQuestions:number;correctAnswers:number;wrongAnswers:number;unanswered:number;passed:boolean;timeSpentSeconds:number;finalizedAt:string}
const BASE=(import.meta.env.VITE_API_BASE_URL??'').replace(/\/$/,'');async function req<T>(p:string,i:RequestInit={}){const r=await fetch(BASE+p,{...i,credentials:'include',headers:{Accept:'application/json',...i.headers}});if(!r.ok){const b=await r.json().catch(()=>({message:'تعذر تنفيذ الطلب'}));throw new Error(b.message||'تعذر تنفيذ الطلب')}return r.json() as Promise<T>}
export const attemptClient={start:(assessmentId:string,startKey:string,csrf:string)=>req<{attempt:Attempt}>(`/api/v1/assessments/${assessmentId}/attempts`,{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({startKey})}),get:(id:string)=>req<{attempt:Attempt}>(`/api/v1/assessment-attempts/${id}`),save:(id:string,qid:string,selectedOptionIndex:number|null,markedForReview:boolean,csrf:string)=>req<{attempt:Attempt}>(`/api/v1/assessment-attempts/${id}/answers/${qid}`,{method:'PUT',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({selectedOptionIndex,textAnswer:'',timeSpentSeconds:0,markedForReview})}),submit:(id:string,key:string,csrf:string)=>req<{result:Result}>(`/api/v1/assessment-attempts/${id}/submit`,{method:'POST',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:JSON.stringify({submissionKey:key})}),result:(id:string)=>req<{result:Result}>(`/api/v1/assessment-attempts/${id}/result`)};
